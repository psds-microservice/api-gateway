package constants

// Роли клиентов (для видеостриминга)
const (
	RoleClient   = "client"
	RoleOperator = "operator"
	RoleAdmin    = "admin"
)

// Статусы стрима
const (
	StreamStatusActive  = "active"
	StreamStatusStopped = "stopped"
	StreamStatusError   = "error"
)
