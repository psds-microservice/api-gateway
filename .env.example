# ===========================================
# API Gateway — переменные окружения
# ===========================================
# Скопируйте в .env и задайте значения.
# При запуске приложение загружает .env из текущей директории (если файл есть).
# Конфигурация только из .env (12-factor).
# Если YAML не найден — конфиг собирается из env и дефолтов.
# ===========================================

# --- HTTP / gRPC (dual-режим) ---
# Локальный запуск: 8084 (чтобы не конфликтовать с Kafka UI 8080). Docker: хост 8084 -> контейнер 8080.
HOST=localhost
SERVER_PORT=8084
GRPC_PORT=9094

# --- User Service (gRPC + HTTP proxy /api/v1/auth/) — порты как в docker-compose ---
USER_SERVICE_HOST=localhost
USER_SERVICE_PORT=9090
USER_SERVICE_HTTP_PORT=8080

# --- Backend URLs (reverse proxy; пусто = прокси не подключается). Порты = deployments/docker-compose.yml ---
SESSION_MANAGER_URL=http://localhost:8091
TICKET_SERVICE_URL=http://localhost:8095
SEARCH_SERVICE_URL=http://localhost:8099
OPERATOR_DIRECTORY_URL=http://localhost:8098
OPERATOR_POOL_URL=http://localhost:8094
NOTIFICATION_SERVICE_URL=http://localhost:8092
DATA_CHANNEL_SERVICE_URL=http://localhost:8093

# --- PostgreSQL ---
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_DATABASE=api_gateway
# или альтернативный ключ:
DB_NAME=api_gateway
DB_SSLMODE=disable

# --- Redis (опционально) ---
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# --- Kafka (опционально) ---
# Если gateway когда‑нибудь будет читать/писать события напрямую,
# используем общий брокер из infra/:
# KAFKA_BROKERS=localhost:9092

# --- JWT ---
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRATION=24

# --- Логирование ---
LOG_LEVEL=info
LOG_FORMAT=json
